/// Shader_palSwap(id)


/*
var _id;
_id = argument0;

with _id
{
    if (sprite_index != -1)
    {
        // palIndex = p.roomPals[? palKey];
        pal_swap_set(p.palSpr, palIndex);
        draw_self();
        pal_swap_reset();
    }
}
*/

